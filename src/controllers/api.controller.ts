import { Request, Response } from 'express';

export const shareYouTubeVideos = (req: Request, res: Response): void => {};

export const unshareYouTubeVideos = (req: Request, res: Response): void => {};
